mixin video(data)
    .video&attributes(attributes)
        iframe(src=`https://www.youtube.com/embed/${data.url}` title=data.title allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share' allowfullscreen='')
